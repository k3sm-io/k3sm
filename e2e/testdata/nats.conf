# Golden fixture: the ConfigMap payload TestM2_ConfigMapMount mounts and the pod
# verifies byte-for-byte (the nats-server-config / nats.conf stockkitty feature).
port: 4222
jetstream {
  store_dir: "/data/jetstream"
}
